UA：告诉服务器 “我是什么浏览器”，请求头字符串。
Cookie：服务器给你的身份小纸条，带登录会话；Cookie 池 = 一堆身份纸条。
IP 池：一堆代理出口，防止单 IP 被拉黑。
三者分别对应：浏览器指纹、账号会话、网络来源，三层风控对抗。

在boss直聘场景中 我们调用api也可能遇到被ban风险 这个跟爬虫agent一个原理。

调用palywright（要分析具体是怎么做到抓取jd或者点击的）